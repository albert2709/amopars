# XPath Lead Logger (Chrome Extension)

Расширение по кнопке выполняет XPath-запрос:

```xpath
//div[@class="pipeline_leads__field h-text-overflow"]
```

Далее:
1. Берёт только значения, где есть **ровно 11 цифр**.
2. Сохраняет найденные значения в текстовый файл **без даты/времени внутри файла** (только номера, по одному в строке).
3. Добавляет дату и время в **название файла**, например: `xpath_results_2026-03-11_14-22-05.txt`.

## Установка

1. Откройте `chrome://extensions`.
2. Включите **Режим разработчика**.
3. Нажмите **Load unpacked / Загрузить распакованное**.
4. Выберите папку с этим проектом.

## Использование

1. Откройте нужную страницу сайта.
2. Нажмите на иконку расширения.
3. Нажмите кнопку **«Собрать по XPath и записать»**.

Если найдены значения с 11 цифрами, они будут сохранены в новый `.txt` файл с датой/временем в имени файла.
